extends layout

block content
  h1 #{title} にようこそ

  a(href="./device")
    p 登録済みデバイス一覧
  form(action="./data", method="get")
    |デバイスID  
    input(type="text", name="deviceID")
    input(type="submit", value="取得")